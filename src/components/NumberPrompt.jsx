export function NumberPrompt({ children }) {
  return <div className="number-prompt">{children}</div>;
}

export function WordPrompt({ children }) {
  return <p className="word-prompt">{children}</p>;
}
